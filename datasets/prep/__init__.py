"""Dataset preparation scripts.

Each module provides tools for preparing a specific dataset for use with
slipstream. Run as scripts or import the prep functions directly.

Available prep scripts:

- ``imagenet_subsets``: Build ImageNet-100 and ImageNet-10 caches from
  the full ImageNet-1K cache. See :mod:`datasets.prep.imagenet_subsets`.
"""
